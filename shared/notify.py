# All versions of suruscraper utilize a notify.py but the functionality is variable depending on which branch is checked into
# My complete version of SuruScraper will have capability for Discord, Email, and Browser notifying, with the web app allowing the user to check boxes signaling which ones they want to opt into.

import smtplib